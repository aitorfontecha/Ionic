import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sgta_scraping',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
